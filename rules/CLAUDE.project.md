# <Название проекта> (шаблон проектных правил)

Сохрани как `CLAUDE.md` в корне репозитория (для Codex — `AGENTS.md`, можно симлинком).
Коротко — агент читает это каждую сессию, длинный файл сжигает контекст.

## Что за проект
<Одно-два предложения: что делает, для кого.>

## Стек
<Например: Go + React + MongoDB; или Expo / React Native. Версии, если важны.>

## Как запускать и проверять
- Запуск: `<команда>`
- Тесты: `<команда>`
- Линт/типы: `<команда>`

## Правила
- <Например: не трогай рабочий код без спроса; UI-правки сначала превью.>
- <Деплой: как и куда; коммитить только по просьбе.>
- <Чего НЕ делать: например, не предлагай web-API в React Native.>

## Грабли
- <Известные неочевидные ловушки проекта — чтобы агент не наступал повторно.>
