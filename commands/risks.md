---
description: Проверка перед стартом — вскрыть риски и вопросы ДО начала задачи
argument-hint: [что делаем]
---

Примени скилл `risks` к задаче: $ARGUMENTS (если пусто — возьми из контекста сессии).

Раз команда вызвана явно — шаг «проверь, нужен ли скилл» пропускается: владелец уже решил, что развилки есть. Проходи допрос полностью, даже если задача кажется простой.

Механика описана в `~/.claude/skills/risks/SKILL.md` — этот файл её не дублирует.
